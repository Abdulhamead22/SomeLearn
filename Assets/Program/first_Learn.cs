using System.Collections;
using System.Collections.Generic;
using JetBrains.Annotations;
using UnityEngine;

public class first_Learn : MonoBehaviour
{
    // Start is called before the first frame update
     public bool dead=true;
    void Start()
    {int x=5;
        Debug.Log(++x);

        // int lives = 3;
        
        
    //     while (lives >= 0)
    //     {
    //         if (lives > 1)
    //         {
    // Debug.Log("Lives is " + lives + " Many Lives");
    //             lives--;
    //         }
    //         else if (lives == 1)
    //         {
    //             Debug.Log("Lives is " + lives + " The Last Lives!");
    //             lives--;
    //         }
    //         else
    //         {
    //             Debug.Log("Lives is " + lives + " Game Over");
    //             break;
    //         }
    //     }
     }


}
